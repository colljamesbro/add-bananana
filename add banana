-- Example for handling input in a Lua script

-- Variable to store bananas (update this to match your game variable)
local currentBananas = 417576

-- Function to add bananas
local function addBananas(amount)
    currentBananas = currentBananas + amount
    print("Bananas added! You now have " .. currentBananas .. " bananas.")
end

-- Function to listen for key presses
local function onKeyPress(key)
    if key == "0" then
        addBananas(100000)
    end
end

-- Example input handling system (change this for your game engine)
local function simulateKeyPress()
    print("Simulating key press listener. Press 0 to add bananas.")

    -- Simulate an input system that waits for user input
    while true do
        local key = io.read() -- Reads user input from the console
        if key == "0" then
            onKeyPress("0")
        else
            print("Press 0 to add bananas!")
        end
    end
end

-- Run the key press simulation
simulateKeyPress()
